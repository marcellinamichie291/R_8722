npm install -g typescript
npm update -g typescript
tsc -v

свойству innerHTML передается строку

cd 
ls 
tsc — компиляция файла в js
tsc -w app.ts — автокомпиляция файла в js "--watch"
tsc app.ts --removeComments — компиляция с удалением комментов
tsc --outFile имя3.js имя2.ts имя1.ts — компиляция в один файл
--noEmitOnError — не компилировать при ошибке или в tsconfig.json указать "noEmitOnError": true,

{} — область видимости let и const видно только внутри, а var видно даже снаружи.