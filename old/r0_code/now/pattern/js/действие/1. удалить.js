// удалить известную переменную найдя её индекс
arr.splice(arr.indexOf(rMax), 1);

// удалить дубликаты из массива //удалить повторяющиеся элементы массива
	массив.filter((item, pos) => массив.indexOf(item) == pos)
    //пресобрать в новый массив, если в масиве еще нет числа то мы его туда положим
	for (const i of names) r0.includes(i) ? 0 : r0.push(i);
// удаление одного елеммента из массива
	for (let i = 0; i < r2.length; i++){if(){delete r2[i]}
		r2.filter(Number);

// удаление елеммента из массива
	for (let i = 0; i < numbers.length; i++){if(){r0.splice(i, 1)}

//строку в мас, удаление ел, мас в сторку
	return s.split('!').join('');
	return s.replace(/!/g, '');

// можно удалить лично не выискивая ел в масс ????
	array.reduce((a,b) => a+b) - rMax - rMin

//удаление rElement в масиве
  for (let i = 0; i < rArr.length; i++) {
    if (rElement === rArr[i]) {rArr.splice(i, 1); break;

// удаление первый и последний еллемменты массива
	rResult.shift();
	rResult.pop();

// filter ???? не удалилло
	rArr = rArr.filter(rEl => rEl !== del);

// удалить пробелы ???? split is not a function
	rArr = str.split(' ').filter(n => n);

// удалить пробелы в начале и в конце строки
	str = str.trim();

//удалить с конца '!'
for(let i=s.length-1; i>=0; i--)
	if(s[i]=="!"){новый=новый.slice(0,-1)}else{break};