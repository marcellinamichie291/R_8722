<!-- квадрат и логарифм10 -->
select sqrt(number1) as root, log(number2) from decimals

<!-- шестнадцатеричное преобразование  -->
TO_HEX(

<!-- округление вниз и вверх -->
floor(переменная), CEILING(переменная)

